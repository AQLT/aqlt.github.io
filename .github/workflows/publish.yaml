# on:
#   workflow_dispatch:
#   push:
#     branches: master
#
# name: Quarto Publish
#
# jobs:
#   build-deploy:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     steps:
#       - name: Check out repository
#         uses: actions/checkout@v3
#
#       - name: Set up Quarto
#         uses: quarto-dev/quarto-actions/setup@v2
#
#       - name: Render and Publish
#         shell: bash
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           git config --global user.email "quarto-ga@example.com"
#           git config --global user.name "yeeeeeeah"
#           quarto publish --profile gh-pages
#
